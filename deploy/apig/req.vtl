#set($inputRoot = $input.path('$'))
{
  "RequestItems": {
    "events" : [
#foreach($e in $inputRoot)
      {
        "PutRequest": {
          "Item": $input.json("$[$foreach.index]")
        }
      }#if($foreach.hasNext),#end
#end
    ]
  }
}
