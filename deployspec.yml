version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - echo "\$CI=$CI"
      - ls -al
      - git version prepatch
      - npm i
      - cd deploy
      - ./deploy.sh
