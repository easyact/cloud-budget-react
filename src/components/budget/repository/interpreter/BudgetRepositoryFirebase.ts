export class BudgetRepositoryFirebase /*implements BudgetRepository*/ {
    // assets = (user: string, version: string) => () => Promise.resolve([]);
    //
    // liabilities(user: string, version: string): Promise<any[]> {
    //     return Promise.resolve([]);
    // }
    //
    // query(user: string, version: string): Promise<Budget | null> {
    //     return Promise.resolve(null);
    // }

}
